# Per-repo values for the shared ../devtools scripts (see
# ../devtools/templates/devtools.conf.sample for every key).
REPO_NAME=remotex
CI_ENV_PREFIX=REMOTEX
WINCI_STACK=rust
WINCI_EXTRA_EXCLUDES="./frontend/node_modules ./frontend/dist ./build"
