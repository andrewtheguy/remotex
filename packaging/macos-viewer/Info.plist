<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleIdentifier</key>
	<string>dev.remotex.viewer</string>
	<key>CFBundleName</key>
	<string>remotex-viewer</string>
	<key>CFBundleDisplayName</key>
	<string>remotex</string>
	<key>CFBundleExecutable</key>
	<string>remotex-viewer</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleVersion</key>
	<string>0.0.0</string>
	<key>CFBundleShortVersionString</key>
	<string>0.0.0</string>
	<key>LSMinimumSystemVersion</key>
	<string>26.0</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.utilities</string>
	<key>NSHighResolutionCapable</key>
	<true/>
	<!-- A gateway is commonly reached directly over plain HTTP on a private
	     network, and the viewer now speaks to it with URLSession rather than
	     inside a WKWebView, so the web-content-scoped exception no longer
	     applies to anything. ATS treats ws:// as http://, so the session socket
	     needs this too. Not scoped with NSExceptionDomains because the address
	     is whatever the user types on the login screen and cannot be enumerated
	     here. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
</dict>
</plist>
