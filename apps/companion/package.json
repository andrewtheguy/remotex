{
  "name": "remotex-companion",
  "private": true,
  "version": "0.0.0",
  "description": "RemoteX Companion: the Chrome extension beside the remotex web client",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "pack": "bun run scripts/pack.ts",
    "test": "bun test tests",
    "check": "biome check . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.tools.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.3.15",
    "@types/bun": "^1.3.14",
    "@types/chrome": "^0.0.287",
    "@types/node": "^26.1.2",
    "typescript": "~5.9.0"
  }
}
