{
  "name": "remotex-viewer",
  "private": true,
  "version": "0.0.0",
  "description": "remotex.app: the macOS shell around the remotex client",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "start": "bun run scripts/build.ts && electron dist/main.cjs",
    "dist": "bun run scripts/dist.ts",
    "test": "bun test tests",
    "check": "biome check . && tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.3.15",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.2",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "typescript": "~5.9.0"
  }
}
